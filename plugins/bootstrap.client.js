// ~/plugins/bootstrap.client.js

import 'bootstrap/dist/js/bootstrap.bundle.min.js'

// You need to wrap your plugin code in defineNuxtPlugin and provide a default export.
export default defineNuxtPlugin(() => {
  // Any code here will run on the client-side when the plugin is loaded.
  // For simply importing Bootstrap's JS, you don't need much more than the import itself.
  // You can optionally add console.log here to confirm the plugin runs.
  // console.log('Bootstrap client plugin loaded and running!');
})